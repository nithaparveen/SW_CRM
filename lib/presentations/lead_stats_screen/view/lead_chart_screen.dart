import 'package:flutter/material.dart';

class LeadChartScreen extends StatelessWidget {
  const LeadChartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
